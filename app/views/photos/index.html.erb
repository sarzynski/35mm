<% @photos.each do |f| %>
<div class = "white">
  <br>
  <%= image_tag f.file.url(:medium) %>
    <% if current_user %>
  <%= button_to "Delete", photo_path(f),  :class=>"inbox-sf-add-btn tip", method: :delete%>
  <% end %>
  <%= f.description %>
</div>
<br><br><br>
<% end %>
