<h1>New Photo</h1>

<%= form_for @photo, html: { multipart: true } do |f| %>
  <%= f.label :file %>
  <%= f.file_field :file %>
  <br>
  <%= f.label :description %>
  <%= f.text_area :description %>
  <br>
  <%= f.submit %>

<% end %>
